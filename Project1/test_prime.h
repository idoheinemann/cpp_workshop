#pragma once

/**
* checks if the given number is prime
*/
bool is_prime(unsigned int num);

/**
* fills the given array with unique prime numbers
* @param arr: the array
* @param len: amount of numbers to fill in
*/
void fill_with_primes(int* arr, unsigned int len);